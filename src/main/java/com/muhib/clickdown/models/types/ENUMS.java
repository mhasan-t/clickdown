package com.muhib.clickdown.models.types;

public class ENUMS {
    public static enum Role {
        ADMIN,
        USER
    }

    public static enum StatusType {
        PENDING,
        IN_PROGRESS,
        DONE
    }

}